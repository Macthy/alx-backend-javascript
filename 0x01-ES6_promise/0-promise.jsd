// Implement the getResponseFromAPI function
export default function getResponseFromAPI() {
  return new Promise((resolve, reject) => {
    // Here, we can implement an asynchronous task if needede
    // For this task, we'll resolve the Promise immediately with a string value
    resolve('Promise resolved successfully');
  });
}
